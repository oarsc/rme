## Creating Docker Image and compiling

Everything is automatized at
```sh
./setup-and-run-docker.sh
```

## Execute

In order to run it locally, these libraries are required
```sh
sudo apt-get install -y libwxgtk3.2-dev freeglut3-dev
```
